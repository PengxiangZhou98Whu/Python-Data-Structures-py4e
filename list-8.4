fname = input("Enter file name: ")
fh = open(fname)
lst = list()
for line in fh:
    line1 = line.rstrip()
    lst1 = line1.split()
    for word in lst1:
        if word not in lst:
            lst.append(word)
lst.sort()
print(lst)


